-- vim: ft=haskell
Config { font = "-misc-fixed-*-*-*-*-10-*-*-*-*-*-*-*"  
       , bgColor = "#090300" --"#1d1f21" --"#1c1c1c"
       , fgColor = "#a5a2a2"
       , position = Top
       --, position = TopSize C 100 35 -- use this if only using one monitor
       --, position = Static { xpos = 0 , ypos = 0, width = 1920, height = 24 } --manually position bar to display on left monitor 
       , lowerOnStart = False
       , allDesktops = True
       , overrideRedirect = False
       , border = BottomBM 0
       , borderColor = "#090300"
       , commands = [ Run Cpu
                          ["-L","3","-H","50","--normal","#01a252","--high","#db2d20"] 10 
                             
        -- network activity monitor (dynamic interface resolution)
        , Run DynNetwork     [ "--template" , "<dev>: <tx>kB/s|<rx>kB/s"
                             , "--Low"      , "1000"       -- units: B/s
                             , "--High"     , "5000"       -- units: B/s
                             , "--low"      , "#a5a2a2"
                             , "--normal"   , "#01a252"
                             , "--high"     , "#db2d20"
                             ] 10
                             
        -- cpu core temperature monitor
        , Run CoreTemp       [ "--template" , "± <core0>°±<core1>°"
                             , "--Low"      , "70"        -- units: °C
                             , "--High"     , "80"        -- units: °C
                             , "--low"      , "#a5a2a2"
                             , "--normal"   , "#01a252"
                             , "--high"     , "#db2d20"
                             ] 50
                          
        -- Disks Args RefreshRate 
        , Run DiskIO [("/", "<read> <write>"), ("sdb1", "<total>")] [] 10    
               
           
        -- volume indicator                    
        , Run Com "/home/mindaugas/.scripts/volume.sh" [] "myvolume" 10 
        
         -- music indicator                    
        , Run Com "/home/mindaugas/.scripts/music" [] "mymusic" 10
        
         -- keyboard layout indicator
        , Run Kbd            [ ("us(dvorak)" , "<fc=#a5a2a2>DV</fc>")
                             , ("us"         , "<fc=#a5a2a2>US</fc>")
                             ]                
                           
                    , Run Date "%a %m/%d %H:%M" "date" 10
                    , Run StdinReader
                    ]
       , sepChar = "%"
       , alignSep = "}{"
       , template = " %StdinReader% }{ %mymusic% * %dynnetwork% * %myvolume% %cpu% %coretemp% * %diskio% * <fc=#01a252>%date% * %kbd%</fc> "
       }
